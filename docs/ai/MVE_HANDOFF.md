# MVE / UX Handoff

This file is owned by the MVE worker in branch `agent/mve-ui`.
The canonical integrator reads it but verifies the referenced commit and test evidence independently.

## Current assignment

Task: `MVE-026-CI-01`
Branch: `agent/mve-ui`
Status: assigned

## Required completion report

- Task ID:
- Branch:
- Commit SHA:
- Base main SHA:
- Changed files:
- User journey changed:
- Visual and interaction decisions:
- Real API endpoints used:
- Loading, empty, error, success states checked:
- Mobile widths checked:
- Accessibility checks:
- Exact test commands run:
- Exact test results:
- Tests not run and reason:
- Backend/API requirements discovered:
- Conflicts with current main:
- Risks and limitations:
- Integrator actions required:
- Recommended next MVE increment:

## Non-negotiable boundaries

- Do not merge into `main`.
- Do not modify database models, migrations, parser runner, storage, or readiness.
- Do not fabricate OCR output, vehicles, repairs, documents, or service history.
- Keep history unchanged until explicit owner confirmation.
- Do not claim mobile or Render behavior without an actual check.