# Backend Handoff

This file is owned by the backend worker in branch `agent/backend`.
The canonical integrator reads it but does not use it as evidence without checking the referenced commit and test output.

## Current assignment

Task: `APP-026-CI-01`
Branch: `agent/backend`
Status: assigned

## Required completion report

- Task ID:
- Branch:
- Commit SHA:
- Base main SHA:
- Changed files:
- Implemented behavior:
- Root cause of each fixed failure:
- Local engineering decisions:
- Exact test commands run:
- Exact test results:
- Tests not run and reason:
- Security/data-integrity checks:
- Conflicts with current main:
- Risks and limitations:
- Integrator actions required:
- Recommended next backend increment:

## Non-negotiable boundaries

- Do not merge into `main`.
- Do not modify `app/static/**` or the owner review page in this task.
- Do not weaken tests merely to obtain green status.
- Do not fabricate OCR/parser results.
- Do not write AI output directly into vehicle history.